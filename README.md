to execute manually execute following commands:
    make compile
    ./cache_simulate <filename>

to execute one file only with name trace<number>.txt execute following commands:
    make execute num = <number>

to execute all trace files:
    make run

to change parameters, change the corresponding variable value in starting lines of makefile.